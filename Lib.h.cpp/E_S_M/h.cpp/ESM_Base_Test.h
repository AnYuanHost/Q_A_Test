#pragma once
#include"ESM_Base.h"

namespace Q_A {
	namespace Code_Test{
		bool NewOrderedPairTestFunction(Element& Domain, Element& Range);

		extern Map NewOrderedPairTest;

		bool ESMbaseNew1TestFunction(Element& Domain, Element& Range);

		extern Map ESMbaseNew1Test;

		bool ESMbaseNew2TestFunction(Element& Domain, Element& Range);

		extern Map ESMbaseNew2Test;

		bool ESMbaseTotalTestFunction(Element& Domain, Element& Range);

		extern Map ESMbaseTotalTest;
	}
}