# KRSPollingDrivenZero_MapSearcher

***

知识库零轮询机映射检索器元素（KRSPollingDrivenZero_MapSearcher）为知识库零轮询机提供一个专有的映射检索器，该元素被初始化为标准的元映射检索元素，方便相关操作。

该元素存储了一组超索引到映射元素的映射关系，用于知识库零轮询机特定工作的映射检索工作。特定工作指零轮询机的内循环。

关于如何使用知识库零轮询机映射检索器元素，参考[KRS_PollingDrivenZero案例](KRS_PollingDrivenZero_Case.md)。
