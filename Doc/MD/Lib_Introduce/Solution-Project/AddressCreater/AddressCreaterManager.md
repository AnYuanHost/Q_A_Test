# AddressCreaterManager

***

集合AddressCreaterManager为所有地址生成器管理的集合，定义为：

    Set AddressCreaterManager(EmptyElement, AddressCreaterManagerTest);

地址生成器管理集合（AddressCreaterManager）由所有地址生成器元素组成，用于地址生成器管理的相关操作。

关于如何使用地址生成管理器，参考[AddressCreater案例](AddressCreater_Case.md)
