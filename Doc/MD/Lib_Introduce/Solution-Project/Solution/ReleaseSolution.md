# ReleaseSolution

***

映射ReleaseSolution接受空元素集合（NullElementSet）为定义域，解决方案集合（SolutionSet）为值域，定义为：

    Map ReleaseSolution(&NullElementSet, &SolutionSet, ReleaseSolutionFunction);

释放解决方案映射（ReleaseSolution）释放值域解决方案元素。

关于如何释放解决方案，参考[Solution案例](Solution_Case.md)
