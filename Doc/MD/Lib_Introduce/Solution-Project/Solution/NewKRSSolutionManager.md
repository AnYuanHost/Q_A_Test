# NewKRSSolutionManager

***

映射NewKRSSolutionManager接受空元素集合（NullElementSet）为定义域和值域，定义为：

    Map NewKRSSolutionManager(&NullElementSet, &NullElementSet, NewKRSSolutionManagerFunction);

新建知识库解决方案管理映射（NewKRSSolutionManager）在值域空元素上默认构造知识库解决方案管理元素。

关于如何完成知识库解决方案管理构造，参考[Solution案例](Solution_Case.md)
