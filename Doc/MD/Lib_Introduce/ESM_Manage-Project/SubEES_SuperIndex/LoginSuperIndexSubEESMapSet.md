# LoginSuperIndexSubEESMapSet

***

映射LoginSuperIndexSubEESMapSet接受空元素集合（NullElementSet）为定义域和值域，其定义为：

    Map LoginSuperIndexSubEESMapSet(&NullElementSet, &NullElementSet, LoginSuperIndexSubEESMapSetFunction);

超索引子元素环境检索器注册指令集映射（LoginSuperIndexSubEESMapSet）在值域空元素上构造一个元素序列作为指令包，并完成相关赋值。

关于如何完成超索引子元素环境检索器指令集注册，跳转至[SubEES_SuperIndex案例](SubEES_SuperIndex_Case.md)，或者[EES_Base案例](..\EES_Base\EES_Base_Case.md)。
