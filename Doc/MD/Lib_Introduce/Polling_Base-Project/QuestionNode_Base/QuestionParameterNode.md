# QuestionParameterNode

***

集合QuestionParameterNode为所有问答节点参数节点的集合，定义为：

    Set QuestionParameterNode(EmptyElement, QuestionParameterNodeTest);

问答节点参数节点集合（QuestionParameterNode）由所有问答节点参数节点组成，用于问答节点参数节点的所有操作。

关于如何使用问答节点参数节点集合，参考案例[QuestionNode_Base案例](QuestionNode_Base_Case.md)
